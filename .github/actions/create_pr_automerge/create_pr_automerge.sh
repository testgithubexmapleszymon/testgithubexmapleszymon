# OUTPUT=$(gh pr create --base=dev01 --header=main)
# gh pr merge "${OUTPUT}" --merge --title "[AUTOMATED] Pull request to dev01" --body "[AUTOMATED] Pull request body"
# gh pr create --base=dev01 --header=main
echo "Hejo"