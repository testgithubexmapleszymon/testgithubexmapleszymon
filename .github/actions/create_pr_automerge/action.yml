name: "Create PR&AutoMerge to Release Branches"
runs:
  using: "composite"
  steps:
    - run: sudo apt update
      shell: bash
    - run: sudo apt install gh
      shell: bash
    - run: script.sh
      shell: bash
