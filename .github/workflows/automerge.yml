name: Auto approve
on:  
  pull_request:
    branches:
      - dev01
      - test01
      - test02

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    steps:
      - uses: hmarr/auto-approve-action@v3
