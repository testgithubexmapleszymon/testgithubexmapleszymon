on:
  pull_request:
  push:
    branches: main

jobs:
  prautomerge:
    runs-on: ubuntu-latest
    name: prautomerge
    steps:
      - name: Check out repository
        uses: actions/checkout@v3
      - name: Execute scripts
        uses: ./.github/actions/prautomerge
